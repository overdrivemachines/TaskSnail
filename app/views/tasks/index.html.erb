<p id="notice"><%= notice %></p>

<div class="my-tasks">
	<% @tasks.each do |t| %>
		<%= render task %>
	<% end %>
</div>

<br>

<%= link_to 'New Task', new_task_path, remote: true, class: "new-task-button" %>
